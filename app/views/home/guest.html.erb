<div class="jumbotron">
  <h1>Welcome</h1>
  
  <p>This project is under development.</p>
  
  <% if current_user.nil? %>
    <%= link_to "Login", new_session_path('user'), class: "btn btn-primary btn-lg" %>
  <% else %>
    <br/>
    <div class="row">
      <div class="col-xs-12 col-sm-6 text-center">
        <%= link_to "Find Deals Now", dashboard_url, class: "btn btn-success btn-lg" %>
        <br/><br/><br/>
    </div>
    <div class="col-xs-12 col-sm-6 text-center">
      <%= link_to "Find a Friend Online", users_path, class: "btn btn-primary btn-lg" %>
    </div>
  <% end %>
</div>